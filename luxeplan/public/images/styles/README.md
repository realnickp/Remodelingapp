# Style reference photos

Style images are **served from** `luxeplan/new styles photos/` (your existing folder). No need to copy anything here.

The app uses: `style1.png`, `style2.png`, `style3.png`, and `style 4.png` through `style 8.png` in that folder, via `/api/style-photo/1` … `/api/style-photo/8`.
